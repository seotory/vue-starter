import {App as IApp} from './interface/app';

class App implements IApp {
    init() {
        // broswer 버젼 체크
        // mqtt 준비
    }

    start(option) {
        // console.log(option);
    }
}

export default new App();